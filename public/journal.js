let posts=[];const $=s=>document.querySelector(s);
function esc(s=''){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function openPost(id){const p=posts.find(x=>x.id===id);if(!p)return;let body=(p.blocks||[]).map(b=>b.type==='quote'?`<blockquote>${esc(b.text)}</blockquote>`:b.type==='image'?`<div class="modal-meta">IMAGE</div>`:`<p>${esc(b.text||'')}</p>`).join('');$('#modalContent').innerHTML=`<div class="eyebrow">${esc(p.category||'ECLAT')}</div><h2>${esc(p.title)}</h2><div class="modal-meta">ECLAT ÉDITIONS</div>${p.excerpt?`<p>${esc(p.excerpt)}</p>`:''}${body}`;$('#modal').classList.add('open')}
async function load(){try{const r=await fetch('/api/posts?section=eclat');if(r.ok)posts=await r.json()}catch(e){posts=[]}$('#eclat-feed').innerHTML=posts.map(p=>`<article class="article" data-id="${esc(p.id)}"><div><span class="article-tag">${esc(p.category||'ECLAT')}</span><h3>${esc(p.title)}</h3><p>${esc(p.excerpt||'')}</p></div><span class="article-date">${p.created_at?new Date(p.created_at.replace(' ','T')+'Z').toLocaleDateString('ru-RU'):''}</span></article>`).join('')||'<p class="section-note">Публикаций пока нет.</p>';document.querySelectorAll('.article').forEach(x=>x.onclick=()=>openPost(x.dataset.id))}
$('#menuBtn').onclick=()=>$('#drawer').classList.add('open');$('#drawerClose').onclick=()=>$('#drawer').classList.remove('open');$('#modalClose').onclick=()=>$('#modal').classList.remove('open');$('#modal').onclick=e=>{if(e.target.id==='modal')$('#modal').classList.remove('open')};$('#searchBtn').onclick=()=>{$('#searchPanel').classList.toggle('open');if($('#searchPanel').classList.contains('open'))$('#searchInput').focus()};$('#searchInput').oninput=e=>{const q=e.target.value.toLowerCase().trim();const all=posts.filter(p=>`${p.title} ${p.category} ${p.excerpt}`.toLowerCase().includes(q)).slice(0,8);$('#searchResults').innerHTML=q?(all.map(p=>`<div class="search-result" data-id="${esc(p.id)}"><b>${esc(p.title)}</b> · ${esc(p.category||'')}</div>`).join('')||'<div class="search-result">Ничего не найдено.</div>'):'';document.querySelectorAll('.search-result[data-id]').forEach(x=>x.onclick=()=>{openPost(x.dataset.id);$('#searchPanel').classList.remove('open')})};load();
